#Display any available message in green
<p style="color: green"><%= notice %></p>

<%= render @category %>

<div>
#Link to edit the current category
#Link to return to the list of categories
  <%= link_to "Edit this category", edit_category_path(@category) %> |
  <%= link_to "Back to categories", categories_path %>
  #Button to delete the current category
  <%= button_to "Destroy this category", @category, method: :delete %>
</div>
